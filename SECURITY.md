# Security

이 스킬은 대상 서비스의 공개 페이지만 읽고, 기본으로 쓰기 작업을 하지 않는다.

- API 키를 저장하거나 전송하지 않는다
- 세션 토큰·쿠키를 산출물에 남기지 않는다
- 원시 HAR는 패키지 밖 임시 파일에만 두고 sanitizer를 거친다
- 메시지 전송, 결제, 로그아웃, 삭제, Reset, 계정 변경은 승인으로도 실행하지 않는다

취약점 제보는 저장소 Issues 또는 유지자 GitHub 프로필로 연락한다.
